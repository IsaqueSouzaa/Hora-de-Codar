//Considerando a figura abaixo, escreva um programa para cada forma que calcule e exiba em tela cada uma de suas respectivas áreas.
          //O usuário irá informar os valores de cada variável.
        
          var maior = parseFloat(prompt(" valor da base maior"));
          var menor =  parseFloat(prompt(" valor da base menor"));
          var h = parseFloat(prompt("valor de H (altura)"))
      var area_trapezio= (( (maior + menor)*h)/2 )
      alert (" O valor da área é "+ area_trapezio);